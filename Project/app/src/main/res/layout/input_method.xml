<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="300dp"
    android:orientation="vertical"
    android:background="#CCF1F8E9"
    android:visibility="visible"> <!-- Pale Translucent Green Keyboard Background -->

    <!-- Smart Draft Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="6dp"
        android:paddingVertical="5dp"
        android:layout_margin="4dp"
        android:background="@drawable/bg_draft_bar_rounded">
        
        <EditText
            android:id="@+id/internal_input_field"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="#2E7D32"
            android:hint="ร่างข้อความที่นี่..."
            android:textColorHint="#802E7D32"
            android:background="@drawable/bg_input_field_rounded"
            android:textSize="14sp"
            android:inputType="textMultiLine"
            android:minLines="1"
            android:maxLines="5"
            android:scrollbars="vertical"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:scrollHorizontally="false" />
            
    </LinearLayout>

    <!-- 1. Top Bar (Toolbar) - Linear Horizontal -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:visibility="visible">

        <!-- Left Side: Music Mode Button -->
        <Button
            android:id="@+id/btn_music_mode"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:text="🎵"
            android:textSize="20sp"
            android:textColor="#4A7C59"
            android:background="@drawable/bg_icon_button_transparent"
            android:contentDescription="Music mode" />

        <!-- Middle: Spacer to push buttons to the right -->
        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <!-- Right Side: Refine and Send Buttons -->
        <Button
            android:id="@+id/btn_refine"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:text="✨"
            android:textSize="20sp"
            android:textColor="#4A7C59"
            android:background="@drawable/bg_icon_button_transparent"
            android:contentDescription="Refine AI"
            android:layout_marginEnd="8dp" />
            
        <Button
            android:id="@+id/btn_send_to_app"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:text="✈️"
            android:textSize="20sp"
            android:textColor="#4A7C59"
            android:background="@drawable/bg_icon_button_transparent"
            android:contentDescription="Send to app" />
                
    </LinearLayout>

    <!-- 2. Suggestion Strip (Clipboard Bar) -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/sunset_gradient">
        <LinearLayout 
                android:id="@+id/suggestion_container"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingHorizontal="8dp">
            
            <!-- Lyric Runner Text repurposed for clipboard empty state -->
            <TextView
                android:id="@+id/lyric_runner_text"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:text="♪ Clipboard empty - copy some text! ♪"
                android:textColor="#4A7C59"
                android:textSize="14sp"
                android:gravity="center_vertical"
                android:paddingHorizontal="12dp"
                android:ellipsize="marquee"
                android:marqueeRepeatLimit="marquee_forever"
                android:singleLine="true"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:selectAllOnFocus="false"
                android:clickable="true" />
                
        </LinearLayout>
    </HorizontalScrollView>

    <!-- 3. Keys Area -->
    <com.example.keyflowai.CustomKeyboardView
        android:id="@+id/keyboard_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:visibility="visible" />

</LinearLayout>